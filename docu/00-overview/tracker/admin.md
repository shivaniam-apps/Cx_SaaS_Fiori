# Admin & Operations — tracker

Worktree `Cx_SaaS_Fiori.worktrees/admin` · CAP 4134 · client 5303 · owns A and T items
(deploy, security, audit, tenancy, CI, docs). Format: see [program-tracker.md](../program-tracker.md).

## In progress

(none)

## To-do (milestone order)

- [ ] T2 Subscription lifecycle: tenant row on subscribe, purge on unsubscribe, dependencies callback (after PO-3)
- [ ] T3 BTP Audit Log service binding (after PO-2)
- [ ] T6 Security review, secret rotation runbook, docu/10, 12, 16
- [ ] T7 Enterprise-volume load test

## Accomplished

- [x] ABAP mirror re-synced wholesale to a4h_2023_zado main df3db96: usage package now carries the S6 extraction parameters, the A9 ZADO_CFG per-tenant secret (table, class, init report) and the S8 inventory reader changes that landed there after the CAP-side PRs — PR #37, 2026-09-17
- [x] T5 Release process: scripts/release.mjs keeps one semver across mta.yaml, the three mtaext files and the server, deployer, client and deploy package files (`current` / `check [--tag]` / `set`), CI runs the check, everything aligned to 0.1.0; .github/workflows/release.yml builds the mtar with mbt on a `vX.Y.Z` tag and attaches it to a GitHub release; /readyz reports the running version; prepare-basic-runtime pins the PostgreSQL deployer to the exact @sap/cds and @cap-js/postgres versions of the build (idea I36); prod.mtaext documents the blue-green testing-phase flow; docu/05 release-process.md (semver rules, checklist, hotfix path, blue-green validation and abort, migration-safety rules), deploy runbook, environments, docu/13 and docu/14 updated; release-script.test.mjs (parsers, CRLF, drift and tag mismatch, repository consistency) — this PR, 2026-09-18
- [x] T4 Cloud Logging + Alert Notification: mta.yaml replaces application-logs with `cloud-logging` (retention 14 days; plan dev in dev.mtaext, standard elsewhere) and binds a new `alert-notification` instance to srv; cds log format json under [production] so Cloud Logging gets level, component and correlation id as fields; utils/alert-notification.js is a fetch-based producer-API client (OAuth client credentials with token cache, basic fallback, unbound = logged only, never throws) and task-runner raises AdoptOpsTaskFailed for FAILED / TIMED_OUT tasks (handler error after the last attempt, silent worker with attempts exhausted, deadline overrun; one event per terminal failure); alert-notification.test.mjs (event shape, unbound, token + POST, basic, every failure mode, runner hook with attempts, broken transport leaves the outcome); docu/05 entitlements and alert-subscription note, environments table, docu/13 new Alerts section with the cockpit recipe and Cloud Logging in the logs table; mbt mtad-gen validates dev and prod — PR #52, 2026-09-18
- [x] A13 Telemetry rate limiting and accessibility pass: utils/telemetry-rate-limit.js (fixed one-minute windows per user and stream with a tenant ceiling, ADOPTOPS_TELEMETRY_{FEEDBACK,ERRORS,BATCHES}_PER_MINUTE 5/30/20 and TENANT_MULTIPLIER 20, 0 disables) wired into submitPilotFeedback (429 with retry hint), recordClientError and recordTelemetryBatch (dropped silently, one log line per window); telemetry-rate-limit.test.mjs (windowing, scopes, tenant ceiling, three HTTP paths); client: src/features/a11y/staticAudit.js scans JSX for controls without an accessible name, 78 findings fixed across 14 pages and two components (accessibleName on every Select/Input/TextArea/DatePicker/Table, aria-label on the hidden file input, decorative tab icon), staticAudit.test.js is the gate in the client suite, `npm run a11y:audit` prints findings; docu/05 knobs, docu/13 telemetry section, docu/14 accessibility gate — PR #48, 2026-09-18
- [x] A12 API coverage: test/api-journey.test.mjs walks the product journey over HTTP through the shared cds.test server in mock-S/4 mode (extraction task -> usage reads and offline import -> analysis task -> proposal decisions with role gates and audit -> waves -> plan derive/simulate/execute with the real task runner -> run monitor, step messages, manifest, operator skip/rollback -> transport release -> AdminService overlay/purge/cleanup/BTP fallbacks -> CoreService userInfo/feedback/telemetry settings -> access requests end to end with MANUAL grant); every PublicService, AdminService and CoreService action/function is now called by at least one suite (33 were uncovered); test/s4-http-client-helpers.test.mjs pins the pure transport helpers (OData path shaping, payload unwrapping, destination identity, headers, redaction, CSRF detection, concurrency gate); 70 new cases, 280 server tests green — this PR, 2026-09-18
- [x] A11 /readyz readiness endpoint: utils/readiness.js probes the database (SELECT 1, five-second deadline, skipped on the database-less tier) and /readyz answers 200 `{status, checks.db}` or 503 `unavailable`; /healthz stays the Cloud Foundry liveness check on purpose (a database outage must not restart-loop the instance); startup console.log/warn moved to cds.log('startup'); readiness.test.mjs (HTTP 200/503 through the shared cds.test server, timeout, not-connected, database-less, never-throws); docu/05 verify section and docu/13 health table updated — PR #40, 2026-09-18
- [x] A10 Pilot documentation: docu/05 deploy-runbook.md (prerequisites, build, deploy, verify, deployer task log, role collections, consumer subscription, redeploy, rollback, runtime configuration reference), docu/13 operations-runbook.md (daily routine, health and logs, background-task states, connection verdicts, activation and transport duties, audit chain, telemetry, access requests, incident quick reference), docu/15 onboarding-a-target-system.md (add-on install, ZADO_CFG_INIT per client, DEV-only SICF node, technical user, Cloud Connector, consumer-subaccount destination, Target Systems fields, Test Connection verdicts, first extraction, per-environment checklist, RD1 values) — PR #38, 2026-09-18
- [x] A9 Per-tenant pseudonymisation salt: ABAP ZADO_CFG + ZCL_ADO_CFG + ZADO_CFG_INIT (secret per client, never transported), ZCL_ADO_PSEUDONYM hashes secret + tenant + user, P_TenantId on ZADO_C_USER_TX_USAGE threaded through reader and export; CAP TenantSecrets random salt replaces the tenant-id salt, usage read passes P_TopUsers/P_MinExecutions/P_TenantId when $metadata declares them; docu/11 GDPR / works-council note for PO sign-off — PR #35 (ABAP: a4h_2023_zado #23, mirror #37), 2026-09-17
- [x] A8 Dead provisioning path removed: provisioning.js, utils/alert-notification.js, utils/cloud-foundry.js (undeclared cfenv / alert-notification-client, missing tenant-automator.js), orphaned isTenantAutomationEnabled and ADOPTOPS_TENANT_AUTOMATION dropped, basic-subscription.js confirmed as the only subscription path, module-load test requires every srv module and checks declared packages — PR #32, 2026-09-17
- [x] A7 Safety defaults: direct S/4 HTTPS calls verify certificates by default (S4_DIRECT_INSECURE_TLS is an explicit, logged opt-in), directTlsOptions unit tests, OData-branch tests proving a payload without ResultJson yields FAILED / SIMULATED_BLOCKED instead of a thrown error, docu/06 direct-access chapter — PR #31, 2026-09-17
- [x] A6 CI gate: .github/workflows/ci.yml (server lint + mocha + production build, client rolldown lock check + lint + node --test + Linux build, tracker check), server eslint config and lint script, server suites renamed to .mjs (module-type warning gone), lock:check script, docu/14 ci-gate chapter — PR #27, 2026-09-17
- [x] A5 Tenant scoping gaps: tenantScoped aspect on the seven remaining entities, tenant-scope helper now recognises service projections (it never matched one before) and filters UPDATE/DELETE too, tenantFilter/stampTenant for direct CQN in access-request, telemetry and settings handlers, per-tenant TelemetrySettings, NULL-tenant backfill on boot, two-tenant cds.test, docu/04 chapter — PR #25, 2026-09-17
- [x] A2 Environment separation: mtaext dev/qa/prod (sizing, plans, CORS origins), mta.yaml free of environment identifiers (S4_DESTINATION removed, CORS_ORIGINS from the approuter URL, saas-registry appName adoptops-${space}), MTA version 0.1.0, router and html5-deployer on Node 22 / html5-app-deployer ^7, docu/05 environments chapter — PR #21, 2026-09-17
- [x] A1 PostgreSQL schema deployment: adops-basic-db-deployer MTA module (gen/pg, cds-deploy CF task, srv ordered after it), postgres cds build task, code/db/package.json, build-time deployer/runtime CSN check, db:ddl/db:deploy:postgres scripts, docu/05 chapter with the verified additive-only migration strategy — PR #18, 2026-09-17
- [x] Integration housekeeping after the first parallel round: A4 tracker line carries PR #13, client lint fixed (unused `userInfo` prop in AdopsShell, idea I13) so the lint gate can go red only for real problems — PR #16, 2026-09-17
- [x] A4 Hash-chained append-only audit log: AuditEvents Sequence/PrevHash/Hash, per-tenant AuditChainHeads lock, one writer (audit-chain.js) for every audit row, @readonly projections + database-level UPDATE/DELETE guard, AdminService verifyAuditChain, IDENTIFIED_USAGE_CHANGED event, docu/10 chapter — PR #13, 2026-09-16
- [x] A3 Read-only PublicService projections, writes via role-gated actions, Admin-only target-system edits, cds.test auth suite — PR #9, 2026-09-16
- [x] Road to Production roadmap checked in as plan of record — PR #8, 2026-09-16
- [x] Parallel worktrees: per-workstream checkouts, port slots, worktree.mjs, with-env `?=` defaults, db:init:sqlite — PR #10, 2026-09-16
- [x] Program tracker, tracker board script, git-workflow rule update, worktree one-pager — PR #11, 2026-09-16
- [x] Worktree session brief: generated CLAUDE.local.md per worktree (add/next/sync), CLAUDE.md worktree section — PR #12, 2026-09-16

## Daily log

### 2026-09-18
- T5 landed on feat/release-process. The version was split three ways (0.1.0 in MTA and client, 0.0.1 in server, deployer and deploy packages); one script now owns it and CI refuses drift. The deployer used caret ranges resolved at staging while the runtime CSN came from the build machine - pinned at build time now (I36 closed). The cf CLI and the MultiApps plugin are not installed on this machine, so the blue-green commands are documented from the plugin reference and the release workflow has not run on GitHub yet: the first `v0.1.0` tag is its acceptance test.
- T4 landed on feat/cloud-logging-alerts. Decisions: no `@sap_oss/alert-notification-client` dependency (A8 removed the undeclared one); a 120-line fetch client against the producer API is enough and testable with an injected binding. `cds env get log.format --profile production` prints plain although the runtime resolves json under NODE_ENV=production - the CLI does not apply the root-level [production] block, verified in node. Subscriptions (who is paged) cannot be deployed with the MTA; they are cockpit configuration and the runbook says so. Idea I42: alerts for the audit chain, readiness and EXPOSED verdicts.
- A13 landed on feat/telemetry-rate-limit-a11y. Neither AdoptOps nor ChronoPilot had any ingestion rate limiting; the limiter is in-process per instance (documented). The accessibility pass is driven by a text scanner over the JSX (no DOM in the client tests), which found 78 unlabeled controls: every filter Select/Input sat next to a visible Label without `for`, no Table had a name, the offline-import file input had no label. All fixed with accessibleName (the visible label text) so screen readers announce the control; the scanner is now the gate. Test hygiene again: probe rows written by tenant-less users are GLOBAL and broke tenant-scope.test.mjs until the suite cleans up after itself.
- Third parallel round merged: #38 A10, #39 O6, #40 A11, #41 S10, #42 O8, #43 A12 (46 files, ~5.5k lines) with every gate green on main (306 server, 123 client, both lints, rolldown lock); ABAP mirror in sync with a4h_2023_zado #25. Housekeeping PR #44: duplicate idea IDs renumbered (second I16/I17/I19/I27/I28 -> I36-I40), promoted I19(now I38)/I21/I26 -> S11, I2/I14/I30 -> O9, I7 -> O10. Worktrees re-pointed: admin A13, scheduling S11 (S3 part 2 stays blocked on RD1), overview O9.
- A12 landed on feat/api-test-coverage. The coverage matrix (grep of every action/function name over test/) showed 33 of 59 service operations without any test before; now none. Findings: (1) enqueueTask polls immediately (setImmediate), so "execute while a task is open" cannot be reached by two HTTP calls in mock mode - the suite plants a live-looking RUNNING task row instead; (2) journey users have no tenant, their rows are GLOBAL and visible to every tenant, which broke the later tenant-scope suite until the journey cleans up its access request; (3) safeResponseData masks only the first word of a header value (`Bearer <token>` leaks the token) - idea I35; (4) one generated proposal per run is SUPERSEDED (no Fiori equivalent), so "all open" assertions must add noEquivalent.
- A11 landed on feat/readyz (cut after #38 merged). Decision: the MTA health check keeps /healthz; /readyz is for operators and monitors, so Cloud Foundry never restarts an instance because PostgreSQL is down. `db.run('SELECT 1 AS ok')` is the same probe check-bindings.js already used. Test gotcha: passing `db: undefined` to a function with a default parameter substitutes the default (cds.db), so the unit tests pass `null`.
- A10 written as three runbooks, each verified against the code it describes (mta.yaml, env.js knobs and defaults, task-runner states, checkTargetSystemConnection stages, ZCL_ADO_ACT_HTTP publication note, ZADO_CFG_INIT). Findings while writing: the tenant route for a subscribed subdomain must be mapped by hand (`cf map-route`) because the subscription callback only returns the URL (idea I34, belongs to T2; renumbered twice, I30 and I32 were taken by the overview and scheduling workstreams the same day); no confirmed authorization-object list exists for the S/4 technical user (left to T6 with an ST01-trace instruction); SystemInfo cannot yet confirm the pseudonym secret (I29 stays open). A5-A9 Accomplished lines now carry their PR numbers (#25, #27, #31, #32, #35).

### 2026-09-17
- A9 built. ABAP part lives on a4h_2023_zado branch feat/tenant-salt (own git worktree; the sibling checkout was on another session's branch and was not touched) and still needs the RD1 DEV/100 smoke test (ZADO_CFG_INIT, UserTransactionUsage(P_TopUsers=20,P_MinExecutions=1,P_TenantId='x')/Set, ZADO_EXPORT_USAGE) before abap/src is mirrored. The CAP side detects the entity parameters from $metadata, which also closes the CAP half of S6 (scheduling to confirm on RD1). PO sign-off on docu/11 is open. Merged over S6 (#34), whose CAP half arrived meanwhile with a 404 fallback: A9 keeps that fallback and adds P_TenantId only when $metadata declares it. S6 referenced a LOG that A6 had removed from s4-fiori-adapter.js (its fallback path would have thrown); defined again here.
- A8 landed. Nothing referenced the three modules; the new module-load suite would have flagged them (three of 38 modules failed to require). Noticed while confirming the live path: server.js registers the SaaS subscription callbacks only for the basic tier, so a standard-tier instance answers 404 to the registry (idea I27, belongs to T2).
- A7 landed. The undefined `body` half of the item had already been removed by S4 (#26); A7 adds the tests that pin that path (OData root, mocked transport, step and probe) and flips the TLS default. Local scripts use a plain-http override for the A4H lab box, so nothing local needed the opt-in.
- A6 landed. Branch protection stays unavailable, so "a red check blocks the PR" is a reviewer rule, not a GitHub setting. Server lint surfaced 17 findings: one was the A7 `body` reference in s4-activate-adapter.js, which S4 (#26) replaced on main before this PR merged (A7 still owns the test and the TLS default), six sit in the dead provisioning path (excluded until A8 removes it), the rest were dead assignments. `npm install` for the new dev dependencies replaced this worktree's node_modules junction with a real directory; the primary checkout was untouched. First CI run caught a peer-dependency clash a local `npm install` tolerates but a clean `npm ci` refuses: @sap/cds 8.9 pins @eslint/js ^9, so the server lint stays on eslint 9.
- A5 landed. Two findings: (1) registerTenantScope checked `target.name.startsWith('adops.db.')`, but an OData request targets the service projection, so no read had ever been filtered; the helper now follows projections to their source. (2) cds deletes the `tenant` attribute of mocked users while multitenancy is off, while the XSUAA strategy always sets `ctx.tenant` from the zone id; the two-tenant test assigns tenants in a post-auth middleware instead of enabling MTX.
- A2 landed (stacked on A1; merge A1 first, then rebase). Per-space values live in mtaext files applied with `cf deploy -e`; `mbt mtad-gen -e` validated all three merged descriptors. No destination name appears in any descriptor: TargetSystems rows own their destinations. The CF login token had expired, so the dev space's current instances were not inspected; the saas-registry rename is only safe because nothing had been deployed there yet (confirm before the first deploy).
- A1 landed. Verified with `cds deploy --dry --delta-from`: schema evolution adds tables, columns and views and widens types, but the compiler refuses dropped elements, dropped tables and length reductions outright ("not supported"), so the deployer cannot lose data and destructive changes need the manual path in docu/05. `mbt build` cannot run in a worktree whose client node_modules is a junction (its `npm ci` would empty the primary checkout's modules); `mbt mtad-gen` and `mbt module-build -m adops-basic-db-deployer` cover the packaging check instead.

### 2026-09-16
- Roadmap approved (single-tenant RD1 pilot first). Surveys found: no Postgres schema deployment, two security blockers (writable projections, no audit chain), live activation stops at ABAP step 2.
- A3 landed. Lesson: cds keeps the [development] profile active under NODE_ENV=test; API tests must pin `cds.env.requires.db` to in-memory and assert it, or they write into the developer's db.sqlite (happened once, two fixture rows removed).
- Worktrees created for admin / scheduling / overview. Lesson: `cds serve --watch` children on this machine stop serving HTTP after a reload; all launch configs use the no-watch script.
- Session start automated: each worktree gets a generated CLAUDE.local.md brief (Claude Code reads it with CLAUDE.md), so no start prompt has to be pasted and a forgotten prompt cannot cause a wrong-worktree session.
- A4 landed. Design notes: a table-level UNIQUE via `@sql.append` compiles to invalid DDL (lands after the closing parenthesis) on sqlite and Postgres, so the sequence is serialised through a per-tenant head row read FOR UPDATE plus an in-process mutex instead. `writeAdminAuditEvent` no longer swallows failures: the audited change and its audit row now commit or roll back together. Pre-A4 rows in dev databases stay as "unchained" legacy events.

### 2026-09-17
- First parallel round merged: #13 A4 (admin), #14 S2 (scheduling), #15 O1 (overview); all suites green on main (111 server, 49 client); ABAP mirror has zero drift against a4h_2023_zado after its PR #18. Worktrees re-pointed to A1 / S1 / O4.
- Second parallel round merged: #16-#36 (21 PRs, ~9.6k lines) with all suites green on main (202 server, 93 client, lint clean). Server lint needs the new eslint dev dependencies: run `npm install` in code/ after pulling. Mirror drift found in abap/src/usage (a4h PRs #22-#24) and re-synced; worktrees re-pointed to A10 / S3 part 2 / O6.
