# Scheduling — tracker

Worktree `Cx_SaaS_Fiori.worktrees/scheduling` · CAP 4124 · client 5293 · owns S items
(background tasks, S/4 adapters, activation engine, ABAP mirror `abap/src`).
ABAP changes start in `../a4h_2023_zado`, are smoke-tested on RD1 DEV/100, then mirrored
wholesale into `abap/src`. Format: see [program-tracker.md](../program-tracker.md).

## In progress

- [~] S9 Catalog derivation: ZADO_CATALOG package + CATALOG_DERIVATION task handler writing BackendCatalogApps / BackendLaunchpadContent (after PO-1) — split: part 1 (CAP pipeline, mock derivation, catalog endpoint, ZADO_PROBE_CATALOG) on branch feat/catalog-derivation, started 2026-09-18; part 2 (ABAP readers from the probe output + mapping source) after the RD1 probe run and PO-1 (part 1 merged PR #59, 2026-09-18: derivation pipeline, catalog endpoint, mock derivation, ZADO_PROBE_CATALOG; part 2 waits for the probe output from RD1 DEV/100 and PO-1)
- [~] S3 Implement ABAP step types returning not_implemented (ACTIVATE_ODATA_SERVICE, CREATE_SPACE, CREATE_PAGE, ASSIGN_PAGE_TO_SPACE, ADD_SPACE_TO_ROLE, ASSIGN_BUSINESS_CATALOG, ADD_CATALOG_TO_ROLE) + transport-append step; verify-first / verify-after / one commit per step — XL, needs RD1 DEV/100 — part 2 on branch feat/abap-activation-steps-2, started 2026-09-17 (part 1 merged PR #23, 2026-09-17: transport-first sequencing, APPEND_TO_TRANSPORT step, plan TRKORR threaded into keys, ZADO_PROBE_ACTIVATION report; part 2 = the seven executors, blocked until the probe output from RD1 DEV/100 is available)

## To-do (critical path first)

- [ ] S12 Transport verification depth (promoted I31, I32): read the tp return code (RC 4/8/12) per import via TMS_MGR_READ_TRANSPORT_HISTORY / ALOG behind the read unit so IMPORT_FAILED is detected, not only recorded; add read-unit entities for the verification kinds that stay MANUAL (ICF node active from ICFSERVICE, OData service active from /IWFND/ tables, space / page existence) so verifyTransportImport covers them - ABAP first in a4h_2023_zado, probe report before executors, as S10 did

## Accomplished

- [x] S7 ABAP usage snapshot collector: ZADO_SNAP_USER / ZADO_SNAP_TX / ZADO_RUN / ZADO_AUDIT tables, ZCL_ADO_COLLECTOR behind report ZADO_COLLECT_USAGE (SM36 monthly), ZCL_ADO_SNAP_READER; providers serve snapshots when the period is covered, live ST03N stays the bounded fallback; CAP records the data source per extraction and the Target Systems page shows snapshot coverage (O17) — PR #55, 2026-09-18
- [x] S11 Configuration hardening: no hardcoded S/4 destination (DEFAULT_DESTINATION = ADOPTOPS_S4_DESTINATION or empty; callS4Destination refuses an empty name before any lookup, destinationNameOf names the target system, checkTargetSystemConnection answers a DESTINATION verdict), local CORS default on the five client port slots, pure utils/config-hardening.js scanning the dev-only variables (S4_URL_OVERRIDES, S4_DIRECT_USER/PASSWORD, S4_DIRECT_INSECURE_TLS, MOCK_S4) under all env prefixes - warned at startup everywhere, refused in a Cloud Foundry instance; docu/05 + docu/06 updated — PR #45, 2026-09-18
- [x] S10 Transport QA/PROD verification: ZADO_C_TRANSPORT_STATUS (E070/E07T/E071, Trkorr filter required) exposed as TransportStatus in the read unit; verification kinds behind the manifest (TRANSPORT_IMPORTED via E070 on the follow-on system, ROLE_EXISTS / ROLE_HAS_USERS via the S8 RoleInventory, MANUAL for spaces/pages/ICF/OData/task lists, failed reads -> UNKNOWN); TransportImports one row per transport and follow-on system with source READ_UNIT / OPERATOR and verdict counts; actions verifyTransportImport / recordTransportImport / readTransportImport, audit TRANSPORT_IMPORT_CHECKED / _RECORDED; Transports page Imports column, Verify and Record import dialogs — PR #41, 2026-09-18
- [x] S8 Roles/users readers: four ZADO custom entities paged at the database (ZADO_C_USER_INVENTORY on USR02 with per-page role counts, ZADO_C_ROLE_INVENTORY on AGR_DEFINE with menu/S_TCODE/user counts and composite/derived typing, ZADO_C_ROLE_USERS on AGR_USERS, ZADO_C_ROLE_TCODES on AGR_TCODES + AGR_1251 by Source) exposed in ZADO_USAGE_SRV, same pseudonym as the ST03N reader; extraction sources USR02 + AGR (default on) fill UserInventory, RoleInventory, RoleUsers, RoleTransactions with IsActiveDialogUser / RoleCount / DistinctTcodeCount rollups; mock inventory coherent with mock usage; taken before S7 (XL) because it unblocks O6 — PR #36, 2026-09-17
- [x] S6 topUsersPerTcode / minExecutions forwarded to ABAP: ZADO_C_USER_TX_USAGE gains CDS parameters P_TopUsers / P_MinExecutions (read by ZCL_ADO_Q_USER_TX, applied in ZCL_ADO_ST03_READER threshold-first then top-N, cache key includes both), the adapter reads `UserTransactionUsage(P_TopUsers=..,P_MinExecutions=..)/Set` and falls back to the plain set with a run-log WARN on a 404 from an older add-on; mock page mirrors the bounds; ZADO_EXPORT_USAGE gains the threshold parameter — PR #34, 2026-09-17
- [x] S5 Operator skip and rollback: `skipActivationStep` (operator skip satisfies dependents, resume runs past it) and `rollbackActivationStep` (ROLLBACK_<StepType> through the write unit, dependents re-opened to PENDING, plan PARTIAL; irreversible ICF/task-list steps are audit-only) with OperatorAction/Note/At/By on ActivationSteps, audit-chain events, `activation-operator.js` predicates mirrored in the client `runModel`, Skip / Roll back / Record rollback on the Activation Runs step rows; ABAP ROLLBACK_CREATE_PFCG_ROLE -> `delete_role` (Z_ADO_* only, dynamic FM candidates, verify-after) — PR #30, 2026-09-17
- [x] S4 Live simulation: read-only state probe ZCL_ADO_ACT_PROBE behind the ICF handler (`probe: true`) and the RAP action (`Probe`), same StepType + ObjectKeyJson contract, verdicts SIMULATED_OK / WARN / BLOCKED with existsAlready from ICFSERVICE, AGR_DEFINE, E070/E071; steps without an executor and incomplete keys are BLOCKED at plan time; CAP adapter probeStepRemote / liveSimulationProbeFor, async simulateSteps, simulateActivationPlan probes live per target system; smoke "Probe only" checkbox — this PR, 2026-09-17
- [x] S1 Connection check covers the activation service: per-endpoint verdicts (USAGE read service, ACTIVATE write unit) in checkTargetSystemConnection / getBackendCapabilities, activation judged against the environment (reachable on DEV, UNPUBLISHED required on QA/PROD, EXPOSED = safety finding), persisted in TargetSystems.lastCheckEndpointsJson, badges on the Target Systems page — PR #20, 2026-09-17
- [x] S2 ObjectKeyJson contract aligned planner <-> ABAP: `objectKey` single source in activation-plan.js (ICF `{fioriId,url,icfName}` from the catalog BSP, role `{role,text,referenceRoles}`, transport `{text}` / `{trkorr,simulation}`), shared fixture activation-object-keys.json, ABAP per-step key types with fail-fast guards, smoke ICF + custom scenarios, docu/09 object-key-contract — PR #14, 2026-09-17
- [x] ABAP mirror brought to parity with a4h_2023_zado main (DEV-only activation write unit, RAP OData V4 write service, RFC function group) — PR #5, 2026-09-15
- [x] ST03 reader fixes from the RD1 export run (top-users 0 = unlimited, truncated-tcode aggregation, control bytes in JSON) — PR #4, 2026-09-15

## Daily log

### 2026-09-18
- Probe round 1 output received (ZADO_PROBE_ACTIVATION + ZADO_PROBE_CATALOG, RD1 client 400) and filed in docu/06 and docu/08; findings in the API matrix "Run 4" and docu/08. Design corrections: /IAM/ is NOT the Fiori app repository (it is Issue and Activity Management); the app id reaches the backend via TADIR UIAD app descriptors and PFCG OTSERVICE nodes; all three role-deletion FMs the S5 rollback tries are MISSING on RD1 (PRGN_ACTIVITY_GROUP_DELETE is the candidate); the task-list parameter recipe and the direct /IWFND/FM_ACTIVATE_SERVICE path are confirmed; spaces/pages API classes and transport types UIST/UIPG confirmed; SMENSAPNEW has no URL column (how PRGN receives an OT node URL is open); ICFSERVICE answered no row for the lower-case BSP name. Round 2 sections added to both probes (ABAP branch feat/probe-round-2) - to be run in CLIENT 100, since spaces, pages and service assignments were empty in 400. S3 part 2 and S9 part 2 start from the round-2 output.
- S9 part 1 on branch feat/catalog-derivation (both repos). Decision: S9 is tagged "after PO-1", but PO-1 concerns the tcode -> app mapping source (reference library vs shipped overlay), not reading the customer's own catalog tables; the CAP half and a read-only probe carry no legal dependency, so they land now and part 2 (ZADO_CATALOG_SRV readers) waits for the probe output and PO-1. Delivered: CATALOG_DERIVATION task handler (utils/catalog-derivation.js: replace-per-system only after a complete read, 404 from the add-on -> PARTIAL and the last good catalog stays, one availability rule for mock and live rows), deriveBackendCatalog action on an ExtractionRuns row with source CATALOG, adapter reads CatalogApps / LaunchpadContent with the documented contract (docu/08 backend-catalog-derivation.md), CATALOG endpoint in the connection check (MISSING = informational, Target Systems badge "Catalog not published"), TargetSystems.catalogRootPath (additive), Extractions page "Derive Catalog" + catalog run rows, mock derivation from the shipped overlay (zmock_<id> BSPs, so mock plans carry ICF nodes and proposals answer AVAILABLE). ABAP: ZADO_PROBE_CATALOG in src/core (IAM, TADIR WAPA + ICFSERVICE, /IWFND/ registry, /UI2/ catalogs/spaces/pages, AGR_HIER of a business role) - run it on RD1 DEV/100 and attach the output to docu/08 before part 2.
- S7 on branch feat/snapshot-collector (both repos). Collector = ZCL_ADO_COLLECTOR behind report ZADO_COLLECT_USAGE (SM36 monthly): one ST03N month per LUW into ZADO_SNAP_USER (real user ids, as SWNCMONI holds them - pseudonymised at read time with the tenant salt) and ZADO_SNAP_TX (coverage marker), closed months skipped, current + previous month always re-collected, retention via ZADO_CFG SNAPSHOT_RETENTION_MONTHS; ZADO_RUN / ZADO_AUDIT log runs and events. ZCL_ADO_SNAP_READER serves a window with the rollups AT THE DATABASE (GROUP BY, COUNT DISTINCT, HAVING threshold); the two usage providers choose SNAPSHOT when covers(window) else LIVE and report DataSource (filterable to force a source). ZCL_ADO_ST03_READER refactored into read_month_raw + rollup (same semantics, shared by collector and live path). CAP: DataSource on the mappers and on UsageSnapshots, run-log line per ST03N read (WARN when live), SystemInfo coverage fields passed through, pure snapshot-coverage.js. ABAP not compiled here: RD1 acceptance in docu/07 snapshot-collector.md (activate, collect three months by hand, compare SNAPSHOT vs forced LIVE counts, one SaaS extraction, re-run skips closed months).
- S11 on branch feat/config-hardening. Pure `utils/config-hardening.js` carries the judgement (local CORS origins from the five port slots, dev-only variable scan with the three env prefixes, CF detection via VCAP_APPLICATION, the no-destination message); server.js logs warnings and throws at bootstrap when a convenience is set in CF. Decision: refuse only in CF, warn elsewhere - the local hybrid scripts run with NODE_ENV=production and ADOPTOPS_S4_URL_OVERRIDES on purpose, and ADOPTOPS_MOCK_S4 is the normal local mode (CF-refused, never warned about locally). `DEFAULT_DESTINATION` is now the env value or empty; callS4Destination refuses an empty name before the mock short-circuit, destinationNameOf names the target system, checkTargetSystemConnection answers a DESTINATION verdict. Not testable on RD1 by nature; acceptance = a CF deployment with a convenience variable set fails to start with the variable named in the log.
- S10 on branch feat/transport-verification (both repos; ABAP branch feat/transport-status-reader). Verification goes through the READ unit only (the write unit is unpublished on QA/PROD): new ZADO_C_TRANSPORT_STATUS on E070/E07T/E071 (a request exists there only once tp imported it - that presence is the import evidence; no TRKORR filter = empty set, never the whole history) plus the S8 RoleInventory for role existence / user assignment; spaces, pages, ICF, OData, task lists stay MANUAL with the manifest hint. TransportImports keeps one row per transport and follow-on system with the verdict counts; operators record IMPORT_FAILED from STMS since tp return codes are not read (idea I31). Browser check on 5293 in mock mode: released RD1K974247, Verify on RD1 Production -> Imported, 5 verified / 8 manual, Record import -> "recorded" on the row. ABAP not compiled here: RD1 acceptance = syntax check + TransportStatus read on DEV with and without the filter, then Verify before and after importing a smoke request into the follow-on client (docu/09 transport-verification).

### 2026-09-17
- S8 on branch feat/roles-users-readers (both repos). Providers page with ORDER BY + OFFSET + UP TO at the database and cap an unbounded $top at 5000 (zcl_ado_q_util), so AGR_USERS-sized tables never materialise in ABAP memory; counts per page use a range of the page's keys (FOR ALL ENTRIES cannot combine with GROUP BY). Column names relied on without an RD1 check: AGR_AGRS(agr_name, child_agr) for composite detection, AGR_TCODES.type = 'TR', AGR_1251(object, field, low, deleted), AGR_TEXTS(spras, line, text), USR02(ustyp, class, gltgv, gltgb, uflag, trdat) - the syntax check on RD1 is the acceptance gate, then one extraction on PROD with the default sources and the four row counts in the run log. UsesFioriToday and HasFioriCatalog stay false until the FIORI source / S9 exist; FullName/Email stay empty (pseudonymised).
- S6 on branch feat/extraction-parameters (both repos). Custom-entity parameters instead of filter fields, per the roadmap wording; the RAP OData V4 address `<set>(P_..=..)/Set` is what the adapter builds and the 404 fallback keeps an older add-on working with a WARN in the extraction log. Not run against RD1 here: acceptance = extract twice on PROD with minExecutions 1 vs 50 (or top 20 vs 5) and compare the USERTCODE row counts in the run log; the "Source bound: ..." INFO line and the absence of the WARN prove the parameters reached ABAP.
- S5 on branch feat/operator-skip-rollback (both repos). Browser check on 5293 in mock mode with a plan seeded through the API (extraction -> proposals -> wave -> approvals -> plan -> execute; the seed script is the I15 candidate): Record rollback on an ICF step shows the notice and the "rollback recorded" tag with the persisted note; rolling back CREATE_PFCG_ROLE re-opens ADD_SPACE_TO_ROLE / GENERATE_PROFILE / APPEND_TO_TRANSPORT and the plan goes PARTIAL with Resume offered. The role-deletion FM is NOT confirmed on RD1: `delete_role` calls PRGN_RFC_DELETE_AGR / PRGN_DELETE_AGR / PRGN_RFC_DELETE_ACTIVITY_GROUP dynamically and fails with a clear message if none exists; ZADO_PROBE_ACTIVATION section 3 now lists them - acceptance on RD1 = smoke Role, then rollback of that plan step (or Custom ROLLBACK_CREATE_PFCG_ROLE) verified absent in PFCG.
- S4 on branch feat/live-simulation (both repos). The probe reuses the executors' verify-first reads (is_node_active, role_exists, read_status, read_objects), so simulation and execution can never disagree on "exists". Transport failures and foreign payloads map to SIMULATED_BLOCKED - a plan never claims a state it could not read. ABAP not compiled here: RD1 acceptance = syntax check of ZCL_ADO_ACT_PROBE / ZCL_ADO_ACT_HTTP / ZBP_ADO_ACT_I_LOG + abstract entity ZADO_A_ACT_INPUT (new Probe field, republish the service binding), then smoke "Probe only" on Role (exists after the Role scenario) and Simulate on a DEV plan (role/ICF steps report existsAlready, space/page steps BLOCKED until S3 part 2).
- S3 part 1 on branch feat/abap-activation-steps: the plan now creates its transport request before the first transportable write and ends with APPEND_TO_TRANSPORT (verify-first on E071, `zcl_ado_act_cts=>append_missing`); the engine threads the plan's TRKORR into the role and append keys at dispatch (rows keep the planned key), so PFCG records the role on the request. New read-only report ZADO_PROBE_ACTIVATION (src/core) answers the three S3 open checks on RD1: task-list parameter FMs for SAP_GATEWAY_ACTIVATE_ODATA_SERV, /UI2/ FDM space/page API surface + transport object types, AGR_HIER node shape for catalogs and spaces. Part 2 (the seven executors) is written from that output. ABAP not compiled here - RD1 syntax check + smoke Transport -> Role -> Append (second Append run must be SKIPPED) is the acceptance run for part 1.
- S1 done on branch feat/connection-check-activation. New CDS type TargetEndpointCheck, `Endpoints` on TargetConnectionCheck, optional `targetSystemId` on the action (old two-argument calls keep working, usage-only), additive column lastCheckEndpointsJson. Browser check on 5293 in mock mode: DEV row shows Connected / Usage ok / Activation ok, PRD row Connected / Usage ok / Activation unpublished, both persisted across reload. Not exercised against RD1 (no session access); the live path reuses probeActivateService, which the S1 acceptance run on RD1 DEV and PROD will prove.

### 2026-09-16
- Survey result to keep in mind: the planner emits seven step types the ABAP dispatcher hard-fails, so a live run stops at step 2 until S3 lands; S2 is the cheap, high-value precursor.
- S2 done on branch feat/activation-key-contract. ABAP changed in a4h_2023_zado (branch of the same name) and mirrored; the CAP suite now parses the ABAP mirror (dispatcher TYPES + smoke literals) against the fixture, so a drift fails `npm test`. NOT yet smoke-tested on RD1 DEV/100 - no system access from this session; the ABAP syntax check and the four smoke scenarios (Transport, Role, Profile, ICF with the default ushell node) are the acceptance run. ICF URLs stay empty until catalog derivation (S9) fills BackendCatalogApps.BspApplication; the dispatcher fails such steps fast with an explicit message instead of calling HTTP_ACTIVATE_NODE with an empty URL.
