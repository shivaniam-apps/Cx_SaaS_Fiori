{
  "name": "adops-basic-db-deployer",
  "version": "0.1.0",
  "description": "PostgreSQL schema deployer for AdoptOps Basic (runs cds-deploy, then create-indexes.js, as a Cloud Foundry task; see docu/05-deployment-tiers/postgres-schema-deployment.md)",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "cds-deploy && node create-indexes.js"
  },
  "dependencies": {
    "@sap/cds": "^8.9.4",
    "@cap-js/postgres": "^1.10.0"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres",
        "vcap": {
          "label": "postgresql-db"
        },
        "schema_evolution": "auto"
      }
    }
  }
}
